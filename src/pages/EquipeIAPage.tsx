import LystesAiSection from "@/components/LystesAiSection";

export default function EquipeIAPage() {
  return (
    <div className="min-h-screen bg-secondary flex items-center justify-center py-12 px-6">
      <div className="max-w-5xl w-full bg-foreground rounded-[2.75rem] p-6 md:p-8" style={{ overflow: "visible" }}>
        <LystesAiSection />
      </div>
    </div>
  );
}
